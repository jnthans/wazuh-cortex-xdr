# Cortex XDR credentials – never commit
wodle/.secrets

# Wodle runtime state – generated at runtime, not part of the repo
wodle/state.json

# Environment variable files (potential accidental credential store)
.env
.env.*

# Python cache
**/__pycache__/
**/*.pyc
**/*.pyo
